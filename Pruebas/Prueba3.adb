procedure CUENTA () is
    primero:INTEGER:=0;
    segundo:INTEGER:=34;
    tercero:INTEGER:=34;
    
begin	
	while(primero<tercero) loop
		put("Dentro del while");
	end loop;
	put(primero);
	
end CUENTA; 
